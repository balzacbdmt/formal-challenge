import { Icon } from "@iconify/react/dist/iconify.js";

function Loading() {
  return (
    <div className="flex flex-col items-center">
      <Icon icon="eos-icons:loading" fontSize={44} />
    </div>
  );
}

export default Loading;
